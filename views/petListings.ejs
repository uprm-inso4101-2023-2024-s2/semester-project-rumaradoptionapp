<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Listings</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
       
       body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            color: #333;
        }

        .section-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .section-header h2 {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
        }

        .section-header p {
            font-size: 1.2rem;
            color: #666;
        }

        .pet-listing {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }

        .pet-card {
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 30px; 
            width: 350px; 
            text-align: center;
            transition: transform 0.3s ease-in-out; 
        }
        .pet-card:hover {
            transform: scale(1.05); 
        }

        .pet-image {
            width: 100%; 
            height: 200px; 
            overflow: hidden; 
            margin: 0 auto 20px; 
        }

        /* .pet-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        } */ 
        .pet-image img {
            width: auto; 
            height: 100%; 
            object-fit: cover; 
            object-position: center; 
        }

        .pet-name {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }

        .pet-details {
            font-size: 1rem;
            color: rgb(0, 0, 0);
            margin-bottom: 10px;
        }

        .view-details-btn {
            background-color: #67c36a;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .view-details-btn:hover {
            background-color: #45a049;
        }
        .filter-dropdown {
            margin-bottom: 20px;
        }

        .filter-dropdown select {
            padding: 10px;
            font-size: 1rem;
            border-radius: 5px;
        }

        .font-poppins {
            font-family: 'Poppins', sans-serif;
        }
        .filter-tabs {
            text-align: center;
            margin-bottom: 20px;
        }

        .tab {
            display: inline-block;
            margin: 0 15px;
            cursor: pointer;
            padding: 12px 20px;
            background-color: #eee;
            border-radius: 5px;
        }

        .tab.active {
            background-color: #67c36a;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="section-header">
            <h2>Pet Listings</h2>
            <p>Browse through our available pets ready for adoption.</p>
        </div>
        <div class="pet-listing">
            <% pets.forEach(pet => { %>
                <div class="pet-card" data-size="<%= pet.size %>" data-age="<%= pet.age %>" data-type="<%= pet.species %>">
                    <div class="pet-image">
                        <img src="<%= pet.image_url || 'default-image-url.jpg' %>" alt="Pet Image">
                    </div>
                    <div class="pet-name"><%= pet.name %></div>
                    <div class="pet-details">Age: <%= pet.age %> | Breed: <%= pet.species %></div>
                    <a href="/petProfile/<%= pet.id %>" class="view-details-btn">View Details</a>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>