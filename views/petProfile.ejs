<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Profile - <%= pet.name %></title>
    <link href="https://fonts.googleapis.com/css?family=Poppins&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        img {
            width: 80%;
            height: auto;
            margin-bottom: 20px;
        }

        .redirect-btn {
            padding: 10px 20px;
            background-color: #06aa0e;
            color: #fff;
            border: none;
            cursor: pointer;
            margin-top: 20px; /* Adjust margin top to position button lower */
            align-content: center;
        }

        .redirect-btn {
            padding: 10px 20px;
            background-color: #06aa0e;
            color: #fff;
            border: none;
            cursor: pointer;
            margin-top: 20px; /* Adjust margin top to position button lower */
            align-content: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="section-header">
            <h2>Pet Profile</h2>
            <p>Meet <%= pet.name %>, ready for a loving home.</p>
        </div>

        <div id="pet-details">
            <h2 class="pet-name"><%= pet.name %></h2>
            <p>Age: <%= pet.age %></p>
            <p>Breed: <%= pet.species %></p>
            <p>Size: <%= pet.size %></p>
            <p>Weight: <%= pet.weight %> kg</p>
            <p>Personality: <%= pet.personality %></p>
            <p>Sex: <%= pet.sex %></p>
            <!-- Add more pet details as needed -->
            <button type="redirect-btn" onclick="redirect()">Fill Adoption Form</button>
        </div>
        <% if (pet.image && pet.image.image_url) { %>
            <img id="petImage" src="<%= pet.image.image_url %>" alt="Image of <%= pet.name %>">
        <% } else { %>
            <img src="/NoPictureFoundPicture.jpg" alt="Default image for <%= pet.name %>">
        <% } %>
    </div>

    <script>
        function redirect(){
            window.location.href="/fillForm"
        }
    </script>
</body>
</html>

