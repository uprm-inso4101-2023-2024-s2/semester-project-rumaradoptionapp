<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Profile - <%= pet.name %></title>
    <link href="https://fonts.googleapis.com/css?family=Poppins&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        .container {
            display: flex;
            justify-content: space-between; /* Adjusts the spacing between elements */
            align-items: center; /* Aligns items vertically */
            padding: 0 200px; /* Adds padding on both sides */
        }
        #pet-details {
            width: 50%; /* Takes up half of the container width */
            text-align: left; /* Text aligned to the left */
        }
        #petImage {
            width: 666.66px; /* Fixed width for the image */
            height: 500px; /* Fixed height for the image */
            object-fit: cover; /* Ensures the image covers the space without stretching */
            border: 3px solid #67c36a; /* Adds a green border similar to the button color */
        }
        .redirect-btn {
            padding: 10px 20px;
            background-color: #67c36a;
            color: #fff;
            border: none;
            cursor: pointer;
            margin-top: 20px; /* Adjust margin top to position button lower */
        }
    </style>
</head>
<body>
    <div class="container">
        <div id="pet-details">
            <h2 class="section-header">Pet Profile</h2>
            <p>Meet <%= pet.name %>, ready for a loving home.</p>
            <h3 class="pet-name"><%= pet.name %></h3>
            <p>Age: <%= pet.age %></p>
            <p>Breed: <%= pet.species %></p>
            <p>Size: <%= pet.size %></p>
            <p>Weight: <%= pet.weight %> kg</p>
            <p>Personality: <%= pet.personality %></p>
            <p>Sex: <%= pet.sex %></p>
            <!-- Add more pet details as needed -->
            <button class="redirect-btn" onclick="redirect()">Fill Adoption Form</button>
        </div>
        <% if (pet.image && pet.image.image_url) { %>
            <img id="petImage" src="<%= pet.image.image_url %>" alt="Image of <%= pet.name %>">
        <% } else { %>
            <img id="petImage" src="/NoPictureFoundPicture.jpg" alt="Default image for <%= pet.name %>">
        <% } %>
    </div>

    <script>
        function redirect(){
            window.location.href="/fillForm"
        }
    </script>
</body>
</html>
